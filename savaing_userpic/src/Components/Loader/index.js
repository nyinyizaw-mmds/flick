import React from 'react'
import Loadersrc from '../Loadersrc/loading.gif'


const Loader = (props) => (
    <div>
        <img alt="loading..." src={Loadersrc} />
    </div>
)

export default Loader